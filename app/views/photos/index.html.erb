<h3>Photos</h3><br/>

<% if user_signed_in? %>
<p><%= link_to "Upload Photo", new_photo_path %></p>
<% end %>

<% @photos.each do |photo| %>
<ul class="picture-recently col-2">
  <li><a href="photo.image, photo"><%= link_to image_tag photo.image.url(:thumb) %></a></li>
  <%= link_to photo.image, photo %>
      <small><%= photo.created_at.strftime("%B, %d, %Y")%></small></p>
</ul>
  <% end %>
</div>
